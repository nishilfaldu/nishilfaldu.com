packages:
  - "."
  - "packages/*"
allowBuilds:
  # Convex's CLI bundles functions with esbuild; its postinstall links the
  # platform binary. Without this the deploy step fails on a missing binary.
  esbuild: true
